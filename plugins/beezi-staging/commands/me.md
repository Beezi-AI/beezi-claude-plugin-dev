---
description: Show the Beezi accounts linked on this machine and which one analytics reads from
allowed-tools: Bash(node:*)
---

Do NOT read, open, or inspect any files. Run only this command:

`node ${CLAUDE_PLUGIN_ROOT}/scripts/me.mjs`

Report its output verbatim. "Not linked" means no saved authorization; a temporary failure
or a renewal in progress is not a reason to suggest /beezi:login. Never echo any token.
